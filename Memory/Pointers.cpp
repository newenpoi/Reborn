#include "Pointers.h"

#include <iostream>
#include <vector>

using namespace std;

int Pointers::execute()
{
    double* pointer_a(0);

    unsigned int* pointer_b(0);

    // Pointeur vers une chaîne de caractères.
    string* pointer_c(0);

    // Tableau dynamique de nombres entiers.
    vector<int>* pointer_d(0);

    // Un pointeur constant vers un entier.
    int const* pointer_e(0);
    
    return 0;
}